<Window x:Class="Szamologep.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Szamologep"
        mc:Ignorable="d"
        Title="Számológép" Height="450" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="Results" Height="20*" ></RowDefinition>
            <RowDefinition x:Name="Buttons" Height="25*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="10*"></RowDefinition>
                <RowDefinition Height="15*"></RowDefinition>
                <RowDefinition x:Name="MemoryButtons" Height="10*"></RowDefinition>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" x:Name="Operations" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" Foreground="Gray" FontSize="18">Valami</Label>

            <Border Grid.Row="1" Background="Red" x:Name="Result"></Border>

            <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="0, 0, 0, 1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
